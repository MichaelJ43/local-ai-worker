{
  "name": "local-ai-worker",
  "version": "0.1.21",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --port 4173 --strictPort",
    "tauri": "tauri",
    "tauri:build": "node scripts/inject-updater-endpoint.mjs && tauri build",
    "version:bump": "node scripts/bump-version.mjs",
    "test:e2e": "playwright test",
    "test:fixtures": "bash test/scripts/repo_agent_fixture_test.sh"
  },
  "dependencies": {
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-process": "^2",
    "@tauri-apps/plugin-updater": "^2"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "@tauri-apps/cli": "^2",
    "vite": "^5.4.0"
  }
}
