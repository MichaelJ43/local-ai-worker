## Summary

<!-- What does this PR change and why? -->

## Type of change

- [ ] Bug fix
- [ ] New feature / UX improvement
- [ ] Documentation only
- [ ] Tests / CI / tooling
- [ ] Refactor (no user-visible behavior change)

## Checklist

- [ ] `cargo test --workspace` and `cargo clippy --workspace -- -D warnings` pass (if Rust changed).
- [ ] `npm ci`, `npm run build`, and `npm run test:e2e` pass (if frontend or CI changed).

## SemVer hint (optional)

Merged PRs default to a **patch** version bump. To bump **minor** or **major**, include in the PR title: `+(semver:minor)` or `+(semver:major)` (see README → Versioning).

## Notes

<!-- Screenshots, follow-ups, etc. -->
